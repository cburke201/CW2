<!DOCTYPE html>
<html lang="en">
<head>
    {{>header}}
</head>
<body>

    {{>navBar}}

    <h1>{{title}}</h1>

    <div class="text-right">
    <a href="/insertForm" class="btn btn-primary">
        Add a Training Goal</a>

    <a href="mailto:?subject=I wanted you to see this site&body=Check out this site http://localhost:3000/trainingGoals"
    class="btn btn-primary"> Share by Email</a>

    </div>

    <div class="text-right" style="margin-bottom: 1em;">
        {{^user}}
        <a id="login" href="/login" class="btn btn-primary">
            Sign In
        </a>

        <a id="register" href="/register" class="btn btn-primary">
            Register
        </a>
        {{/user}}
        
        {{#user}}  
        <a id="logout" href="/logout" class="btn btn-primary">
            Sign Out
        </a>

        {{/user}}
    </div>
    <br>

    {{#trainingGoals}}
    <div>
        Name of Trainer: <a href="/posts/{{name}}">{{name}}</a>
    </div>
    <div>
        Goal to be Achieved: {{goal}}
    </div>
    <div>
        Length of Goal (in minutes or reps): {{duration}}
    </div>
    <div>
        Scheduled Date: {{date}}
    </div>
    <div>
        Has the Goal been completed: {{completed}}
    </div>
    <a href="/delete/{{_id}}" alt="delete this entry"><i class="fa fa-trash" style="font-size:24px"></i></a>
    <a href="/update/{{_id}}" alt="update this entry"><i class="fa fa-edit" style="font-size:24px" style="margin: 10px"></i></a>
    <hr>
    {{/trainingGoals}}
    {{^trainingGoals}}
    No entries yet
    {{/trainingGoals}}

</body>
</html>